{
  "name": "vue-poc-federation-modules",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "npm": "^8.4"
  },
  "workspaces": [
    "home",
    "app"
  ],
  "scripts": {
    "dev": "concurrently -n home-build,home-server,app -c \"bgBlue.white.bold,bgBlue.white.bold,bgRed.white.bold\" \"npm run build --workspace=home -- --watch\" \"npm run preview --workspace=home\" \"npm run dev --workspace=app\"",
    "build": "npm run build --workspaces --if-present",
    "preview": "concurrently -n home,app -c \"bgBlue.white.bold,bgRed.white.bold\" \"npm run preview --workspace=home\" \"npm run preview --workspace=app\"",
    "stop": "kill-port --port 5000,5001,5002,5003"
  },
  "devDependencies": {
    "concurrently": "^6.1.0",
    "kill-port": "^1.6.1"
  }
}